# Решение задачи Two Sum

## Цель работы

Разработать программу на языке Python для поиска двух элементов массива, сумма которых равна заданному числу.
Также необходимо реализовать модульные тесты для проверки корректности работы программы.

---

## Постановка задачи

Дан массив целых чисел `nums` и целое число `target`.
Необходимо вернуть индексы двух чисел, сумма которых равна `target`.

### Условия задачи

* существует ровно одно решение;
* один и тот же элемент нельзя использовать дважды;
* ответ можно вернуть в любом порядке.

### Примеры

#### Пример 1

```text
Ввод: nums = [2,7,11,15], target = 9
Вывод: [0,1]
```

#### Пример 2

```text
Ввод: nums = [3,2,4], target = 6
Вывод: [1,2]
```

#### Пример 3

```text
Ввод: nums = [3,3], target = 6
Вывод: [0,1]
```

---

## Описание алгоритма

Для решения задачи используется метод полного перебора.

### Алгоритм работы программы

1. Получить длину массива.
2. С помощью внешнего цикла выбрать первый элемент массива.
3. С помощью внутреннего цикла перебрать все элементы после первого.
4. Проверить, равна ли сумма двух элементов значению `target`.
5. Если условие выполняется — вернуть индексы найденных элементов.
6. Если подходящая пара не найдена — вернуть пустой список.

---

## Листинг программы

### Файл `main.py`

```python
from typing import List


class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        n = len(nums)
        for i in range(n):
            for j in range(i + 1, n):
                if nums[i] + nums[j] == target:
                    return [i, j]
        return []
```

---

## Тестирование программы

Для проверки корректности работы были разработаны модульные тесты с использованием библиотеки `unittest`.

### Файл `test_main.py`

```python
import unittest
from main import Solution

class TestSolution(unittest.TestCase):
    def setUp(self):
        self.solution = Solution()

    def test_basic_case(self):
        result = self.solution.twoSum([2, 7, 11, 15], 9)
        self.assertIn(result, [[0, 1], [1, 0]])

    def test_multiple_possible_pairs(self):
        result = self.solution.twoSum([3, 2, 4, 1, 5], 6)
        self.assertEqual(result[0] + result[1], 6)

    def test_negative_numbers(self):
        result = self.solution.twoSum([-1, -2, -3, -4, -5], -8)
        self.assertIn(result, [[2, 4], [4, 2]])

    def test_zero_target(self):
        result = self.solution.twoSum([1, -1, 2, 3], 0)
        self.assertIn(result, [[0, 1], [1, 0]])

    def test_duplicate_numbers(self):
        result = self.solution.twoSum([3, 3, 4, 5], 6)
        self.assertIn(result, [[0, 1], [1, 0]])

    def test_no_solution(self):
        result = self.solution.twoSum([1, 2, 3, 4], 10)
        self.assertEqual(result, [])

if __name__ == '__main__':
    unittest.main()
```

---

## Результаты тестирования

В ходе тестирования были проверены следующие случаи:

| № | Тестовый случай           | Результат |
| - | ------------------------- | --------- |
| 1 | Базовый пример            | Успешно   |
| 2 | Несколько возможных чисел | Успешно   |
| 3 | Отрицательные числа       | Успешно   |
| 4 | Нулевая сумма             | Успешно   |
| 5 | Повторяющиеся элементы    | Успешно   |
| 6 | Отсутствие решения        | Успешно   |


---

## Вывод

В ходе работы была реализована программа для поиска двух элементов массива, сумма которых равна заданному числу.
Для проверки корректности работы были разработаны модульные тесты, охватывающие различные сценарии использования программы.

Программа успешно выполняет поставленную задачу и корректно обрабатывает различные входные данные.
